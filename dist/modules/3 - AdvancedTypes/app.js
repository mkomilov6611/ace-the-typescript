"use strict";
function merge(a, b) {
    if (typeof a === "string" || typeof b === "string") {
        return a.toString() + b.toString();
    }
    return a + b;
}
const result = merge("i", "Mac");
result.split("");
